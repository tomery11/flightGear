<Window x:Class="flightGear.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flightGear"
        xmlns:views="clr-namespace:flightGear.views"
        mc:Ignorable="d"
        Title="Flight Gear" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>

            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <Viewbox Grid.Row="0" Grid.RowSpan="1" Grid.Column="0" Stretch="Fill">
            <views:map/>

        </Viewbox>


        <Viewbox Grid.Row="0" Grid.Column="1">
            <views:dashboard/>

        </Viewbox>

        <Viewbox Grid.Row="1" Grid.Column="1">
            

        </Viewbox>

       

    </Grid>
</Window>
